---
description: 발표자료 스킬의 개인 설정(청중·경어체·색·폰트·말투 보호 목록)을 보여주거나 바꾼다.
---

`~/.claude/skills/pt-deck/profile.md`를 읽고 현재 설정을 표로 보여준다.

사용자가 바꾸고 싶은 항목을 말하면 `profile.md`와 `SKILL.md`의 "이 사용자의 기본값" 표를 **함께** 고친다. 둘이 어긋나면 안 된다.

새 자료를 읽어 달라고 하면 그 파일을 읽고 관찰한 것만 반영한다. 짐작으로 채우지 않는다.

처음부터 다시 세팅하려면 pt-deck-kit 저장소 폴더를 열고 그곳의 `CLAUDE.md` 온보딩을 따르라고 안내한다.

$ARGUMENTS
