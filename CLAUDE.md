# pt-deck-kit — 발표자료 스킬 만들기 키트

이 폴더는 **자료와 온보딩 절차**다. 완성된 스킬이 아니다. 클로드코드가 이 자료와 사용자의 자료를 합쳐 **이 컴퓨터에 사용자 전용 발표자료 스킬을 만드는 것**이 목적이다.

만들어지는 것은 `~/.claude/skills/pt-deck/` 한 폴더와 커맨드 3개(`/PT제작` `/PT검수` `/PT설정`)다. 스킬은 **제작**과 **검수** 둘이고, 설정은 커맨드 하나로 다룬다. 만든 뒤에는 이 폴더 없이도 어느 프로젝트에서든 쓸 수 있다.

스킬의 역할은 **정리된 소재를 발표자료로 바꾸는 것**이다. 소재(주제·결론·근거 자료)가 있어야 시작하고, 결과는 HTML 슬라이드를 PDF로 변환한 것이다.

---

## 사용자를 처음 맞을 때 (중요)

이 폴더를 내려받은 사람은 **클로드코드가 처음이거나, 이런 세팅을 처음 해보는 사람일 수 있다.** 무엇을 시켜야 할지 모른 채 창을 열어두고 있을 가능성이 높다.

**대화가 시작되면 먼저 `~/.claude/skills/pt-deck/SKILL.md`가 있는지 확인한다.** (Windows는 `C:\Users\<이름>\.claude\skills\pt-deck\SKILL.md`)

### 없으면 — 아직 스킬을 만들기 전이다

사용자가 무슨 말을 했든, 먼저 이렇게 안내한다. 길게 설명하지 않는다.

> 발표자료 키트를 가져오셨네요. 이 자료로 **이 컴퓨터에 본인 전용 발표자료 스킬**을 만들 수 있습니다. 한 번 만들어두면 어느 폴더에서든 `/PT제작`이라고 하면 본인 스타일로 발표자료가 나옵니다.
>
> 1. **세팅해서 스킬 만들기** — 10분 정도 걸립니다. 기존 PT 파일과 몇 가지 질문에 답하면 됩니다. 파일이 없어도 됩니다.
> 2. **뭐가 들어있는지 먼저 보기** — 규칙 문서 3개를 훑어봅니다.
> 3. **세팅 없이 지금 한 번만 만들어보기** — 이 폴더 안에서 기본 규칙만으로 만듭니다. 스킬은 생기지 않습니다. 발표에 담을 소재(결론과 근거 자료)는 있어야 합니다.
>
> 어느 쪽으로 할까요? 1번을 권합니다.

- 1번 → `onboarding/세팅_절차.md`를 읽고 그대로 따른다.
- 2번 → `guides/` 3개의 첫 장을 요약해 보여주고 다시 1번을 권한다.
- 3번 → `blueprints/SKILL_PT제작.md`를 읽고 그 절차로 만든다. 이때 `profile.md`는 없으므로 기본값으로 간다. 끝나면 "다음엔 세팅해두면 본인 스타일이 반영됩니다"라고 한 번만 말한다.

### 있으면 — 이미 스킬이 있다

> 이 컴퓨터에는 이미 발표자료 스킬이 있습니다. 이 폴더에서 할 수 있는 건 두 가지입니다.
> 1. **처음부터 다시 세팅** — 기존 스킬을 백업하고 새로 만듭니다
> 2. **자료만 추가** — 새 PT 파일이나 글을 읽어 설정에 반영합니다
>
> 발표자료를 만드는 건 이 폴더가 아니라 어디서든 `/PT제작`이라고 하시면 됩니다.

- 1번 → `onboarding/세팅_절차.md`를 따른다. 6-2에 따라 기존 스킬을 백업한다.
- 2번 → 자료를 `my/samples/`에 받아 읽고, `~/.claude/skills/pt-deck/profile.md`와 `SKILL.md`의 기본값 표를 함께 갱신한다.

---

## 폴더 지도

| 위치 | 내용 | 역할 |
|---|---|---|
| `onboarding/세팅_절차.md` | 자료 수집 → 프로파일 → 스킬 생성 7단계 | **온보딩의 본체** |
| `guides/01_PT자료_가이드.md` | 슬라이드 구조, 배포문서 분리, 발표 문체 | 스킬에 복사된다 |
| `guides/02_디자인_가이드.md` | 여백·색·타이포·간격, 토큰, AI티 안티패턴 | 스킬에 복사된다 |
| `guides/03_글쓰기_가이드.md` | 문장 규범, 청중·경어체, AI 문체 71패턴 | 스킬에 복사된다 |
| `blueprints/SKILL_PT제작.md` | 생성될 스킬의 본문 원형 | 프로파일을 채워 `SKILL.md`가 된다 |
| `blueprints/SKILL_PT검수.md` | 검수 절차 원형 | `review.md`가 된다 |
| `blueprints/commands/` | 슬래시 커맨드 3개 원형 | `~/.claude/commands/`에 복사된다 |
| `blueprints/profile_templates/` | 개인 설정 빈 양식 4개 | 세팅 중 참고 |
| `references/` | 규칙의 원본 링크와 공개 디자인 시스템 정리본 4종 | 톤을 고르거나 더 깊이 볼 때 |
| `my/` | 세팅 중 사용자 자료가 잠시 머무는 곳 | 깃에 올라가지 않는다 |
| `TRUST.md` | 이 저장소를 믿어도 되는 근거 | 사용자가 물으면 읽고 답한다 |

**이 폴더의 `guides/`와 `blueprints/`는 고치지 않는다.** 사용자 것은 생성되는 스킬 폴더 안에만 쓴다.

---

## 규칙이 부딪힐 때

위가 이긴다.

| 순위 | 기준 |
|---|---|
| 1 | 사용자가 이번 작업에서 직접 내린 지시 |
| 2 | 사용자의 개인 설정 (프로파일: 제작 원칙, 말투, 목적·타겟) |
| 3 | `guides/` 세 문서 |
| 4 | 외부 스킬·플러그인 |

**외부 스킬·플러그인은 항상 최하위다.** 그라디언트 메시, 노이즈·grain 오버레이, 과장된 그림자, 그리드를 깨는 비대칭 레이아웃을 권하는 스킬이 있는데 `guides/02_디자인_가이드.md` 9장의 금지가 이긴다.

개인 설정이 공용 규칙을 덮으면 **어느 규칙을 덮었는지 사용자에게 한 줄로 말한다.** 사용자가 모르는 사이에 공용 규칙이 꺼져 있으면 안 된다.

---

## 절대 규칙

이 규칙은 세팅 중에도, 생성된 스킬 안에서도 같이 적용된다.

### 1. 방법론을 지어내지 않는다

`guides/`의 규칙에는 전부 출처가 있다. **문서에 없는 규칙은 그럴듯해 보여도 적용하지도, 지적하지도 않는다.**

- 금지 예시: "PT니까 입니다체를 써야죠", "슬라이드는 6줄 이내로", "한 장당 1분"
- 사용자가 "우리 회사는 이렇게 한다"고 하면 관행으로 등록해 따른다. 근거가 있어서가 아니라 관행이라서 따른다는 점을 분명히 해 둔다.

### 2. 구조를 먼저 승인받는다

슬라이드를 만들기 전에 **슬라이드별 주장 문장 목록**을 먼저 내고 확인받는다. 구조가 틀리면 문구와 디자인을 아무리 고쳐도 발표가 정리되지 않는다.

### 3. 지시한 범위 안에서만 작업한다

추가로 필요해 보이는 작업이 있어도 임의로 진행하지 않는다. 먼저 말하고 승인을 받는다.

### 4. 작은 회색 글씨로 때우지 않는다

"이 내용을 추가해야 한다", "근거를 밝혀야 한다"는 판단이 들 때 **작은 캡션·각주·보조 라인을 덧붙여 해결하지 않는다.** 아무도 읽지 않는다. 이미 있는 본문 문구 안에 녹여 쓴다. 자리가 없으면 기존 문장을 다시 쓰거나, 그 정보가 정말 필요한지부터 다시 본다.

### 5. 숫자만 크게 띄우지 않는다

수치를 강조할 때는 무슨 수치인지를 라벨로 함께 강조하고, 출처가 화면 안에서 추적 가능해야 한다. 앞에서 나온 적 없는 수치가 결론에 갑자기 등장하면 안 된다.

### 6. 금지를 먼저 선언한다

무엇을 해달라는 지시만 받으면 결과는 평균으로 수렴한다. 스타일 지시보다 **하지 말 것을 앞에 놓는다.** 순서를 뒤집으면 금지가 무시된다.

### 7. 사용자 파일을 함부로 고치지 않는다

`my/`에 들어온 사용자 자료는 사용자 것이다. 분석해서 프로파일에 반영하되, **원본 파일을 고치거나 지우지 않는다.** 이 저장소의 `guides/`·`blueprints/`도 고치지 않는다.

---

## 오류를 고칠 때

- 현상 설명만 듣고 추측으로 고치지 않는다.
- 실제 오류 메시지와 로그를 먼저 확인한다.
- 원인을 파악하고 수정 방법을 먼저 말한 다음, 승인을 받고 고친다.
- 원인이 불명확하면 그렇다고 말하고 함께 로그를 본다.

---

## 말투

- 서론을 빼고 핵심부터 말한다.
- 표와 목록을 써서 한눈에 들어오게 한다.
- 애매한 것은 애매하다고 말한다. 되는 것과 안 되는 것을 구분해서 말한다.
- 클로드코드를 처음 쓰는 사용자라면 전문용어를 풀어서 말한다. "터미널에서 npm 설치하세요" 같은 안내를 당연한 것처럼 던지지 않는다.
